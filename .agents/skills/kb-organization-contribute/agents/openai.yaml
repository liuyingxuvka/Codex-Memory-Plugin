interface:
  display_name: "KB Organization Contribute"
  short_description: "Run the unscheduled contribution child phase"
  default_prompt: "Use $kb-organization-contribute as the organization cycle child phase or for one explicit diagnostic contribution pass."

policy:
  allow_implicit_invocation: false
